import { grey700 } from 'material-ui/styles/colors';

const styles = {
    flatButton: {
        color: grey700,
        lineHeight: '0px',
        borderRadius: 0,
    },
    spanstyle: {
        textTransform: 'none',
    },
};

export default styles;
