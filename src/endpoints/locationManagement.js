import { apiUrl } from '../constants/apiConstant';

export default({
    countryPath: apiUrl.BASE + apiUrl.country,
    statePath: apiUrl.BASE + apiUrl.state,
    cityPath: apiUrl.BASE + apiUrl.city,
});
